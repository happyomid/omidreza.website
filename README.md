# وب‌سایت شخصی امیدرضا

این مخزن کد منبع پرتفوی شخصی امیدرضا است؛ وب‌سایتی مدرن که نمونه‌کارها، مهارت‌ها و راه‌های ارتباطی را با تکیه بر Nuxt 4 و طراحی مینیمال نمایش می‌دهد.

## 📌 معرفی
- صفحه خانه شامل بخش قهرمان، معرفی کوتاه، پروژه‌های شاخص، مهارت‌ها و دعوت به همکاری است.
- صفحات مستقل `about`، `projects` و `contacts` برای توضیح کامل‌تر تجربه‌ها، پروژه‌ها و اطلاعات تماس در نظر گرفته شده‌اند.
- داده‌های پروژه‌ها و مینی‌پروژه‌ها از طریق Pinia مدیریت می‌شوند و برای به‌روزرسانی محتوا کافی است ساختار موجود در `stores/projects.ts` را ویرایش کنید.
- از Nuxt UI و Tailwind CSS برای ساخت کامپوننت‌های ماژولار و قابل بازاستفاده بهره گرفته شده است.

## ⚙️ تکنولوژی‌های کلیدی
- `Nuxt 4` و `Vue 3` برای ساخت وب‌اپ مدرن و سریع.
- `TypeScript` جهت افزایش پایداری و خودمستندی کد.
- `Tailwind CSS` و `Nuxt UI` برای طراحی رابط کاربری واکنش‌گرا.
- `Pinia` برای مدیریت حالت و نگه‌داری داده‌های پروژه‌ها.
- `nuxt-swiper` برای اسلایدر پروژه‌های شاخص.

## 🗂️ ساختار پروژه
```text
app/
  components/
    home/          # بخش‌های صفحه اصلی (پروژه‌ها، مهارت‌ها، درباره من، تماس)
    layout/        # سرصفحه، پاصفحه و اجزای تم
    projects/      # کارت‌ها و نمایشگرهای پروژه
    shared/        # اجزای عمومی مانند دکمه‌ها و آیکون‌ها
  pages/
    index.vue      # صفحه خانه
    projects.vue   # لیست کامل پروژه‌ها و مینی‌پروژه‌ها
    about.vue      # معرفی تفصیلی تجربه‌ها و مهارت‌ها
    contacts.vue   # اطلاعات تماس و راه‌های ارتباطی
stores/
  projects.ts      # منبع داده پروژه‌ها و مینی‌پروژه‌ها با Pinia
public/
  images/          # تصاویر هویت بصری و گرافیک‌های استفاده‌شده
nuxt.config.ts     # پیکربندی ماژول‌ها، استایل‌ها و تنظیمات پروژه
```

## 🚀 راه‌اندازی سریع
```bash
# نصب وابستگی‌ها
pnpm install

# اجرای محیط توسعه (http://localhost:3000)
pnpm dev

# ساخت نسخه تولیدی
pnpm build

# پیش‌نمایش خروجی تولیدی
pnpm preview
```
> در صورت تمایل می‌توانید از `npm` یا `yarn` نیز استفاده کنید؛ اسکریپت‌ها با همان نام در دسترس هستند.

## 💡 نکات توسعه
- کلاس‌های Tailwind در فایل `app/assets/css/main.css` سفارشی‌سازی شده‌اند؛ برای تغییر تم یا رنگ‌بندی از همین‌جا اقدام کنید.
- برای افزودن پروژه جدید، یک آبجکت تازه به آرایه `projects` یا `miniProjects` در `stores/projects.ts` اضافه کنید. دکمه‌ها، لینک‌ها و فناوری‌های استفاده شده را می‌توان در همان آبجکت تنظیم کرد.
- در صورت نیاز به کامپوننت جدید، آن را در مسیر `app/components` بسازید و با استفاده از قابلیت Auto Import در Nuxt، بدون نیاز به `import` دستی در صفحات استفاده کنید.

## 📦 استقرار
- دستور `pnpm build` خروجی تولیدی را در پوشه `.output` قرار می‌دهد. این خروجی برای پلتفرم‌هایی مانند Vercel، Netlify یا سرورهای Node.js قابل استقرار است.
- برای میزبانی استاتیک می‌توانید از `pnpm generate` استفاده کنید تا نسخه‌ای بدون نیاز به سرور ایجاد شود.

---

اگر پیشنهادی برای بهبود تجربه کاربری، توسعه ویژگی جدید یا گزارش باگ دارید، یک Issue جدید ثبت کنید یا پیام بفرستید. 🙌🏻
